{% extends 'base.html.twig' %}

{% block title %}Chimpokomon{% endblock %}

{% block body %}
    <h1>Chimpokomon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chimpokomon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chimpokomon.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ chimpokomon.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ chimpokomon.createdAt ? chimpokomon.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ chimpokomon.updatedAt ? chimpokomon.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chimpokomon_crud_index') }}">back to list</a>

    <a href="{{ path('app_chimpokomon_crud_edit', {'id': chimpokomon.id}) }}">edit</a>

    {{ include('chimpokomon_crud/_delete_form.html.twig') }}
{% endblock %}
