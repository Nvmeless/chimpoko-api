<form method="post" action="{{ path('app_chimpokomon_crud_delete', {'id': chimpokomon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chimpokomon.id) }}">
    <button class="btn">Delete</button>
</form>
